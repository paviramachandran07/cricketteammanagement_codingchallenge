export interface Player {
  id?: number; 
  name: string; 
  jerseyNumber: number;
  role: string;
  totalMatches?: number;
  teamName?: string;
  country?: string;
  description?: string;
}
